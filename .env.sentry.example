# Sentry Configuration
# Get these values from: https://sentry.io/settings/[your-org]/projects/[your-project]/keys/

# Required - Public DSN (safe to expose in client-side code)
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Required for source map uploads (keep secret!)
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Organization slug (from Sentry URL)
SENTRY_ORG=your-org-slug

# Project name (from Sentry URL)
SENTRY_PROJECT=heyberkshire-com

# Optional - Environment
NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# Instructions:
# 1. Create a Sentry account at https://sentry.io
# 2. Create a new project (select Next.js)
# 3. Copy the DSN from Project Settings → Client Keys
# 4. Create an auth token at https://sentry.io/settings/account/api/auth-tokens/
#    - Scopes needed: project:releases, project:write
# 5. Copy these values to .env.local for development
# 6. Add to Vercel environment variables for production
